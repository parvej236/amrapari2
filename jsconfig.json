{
  "compilerOptions": {
    "baseUrl": ".",             // <-- required for paths to work
    "paths": {
      "@/*": ["src/*"]          // ./ is optional here
    }
  },
  "exclude": ["node_modules", "dist"]
}
